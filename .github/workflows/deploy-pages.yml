# Deploys the static export to GitHub Pages on every push to main.
#
# One-time setup in the repository: Settings → Pages → Source: "GitHub Actions".
#
# Until a custom domain is configured, the site is served from
# https://<user>.github.io/QCweb/, so the build needs the base path below.
# When a custom domain is attached, set NEXT_PUBLIC_BASE_PATH to "" (or remove
# the env line) and add the domain in Settings → Pages.
name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Build static site
        run: npm run build
        env:
          NEXT_PUBLIC_BASE_PATH: /QCweb

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
